import { cn } from '@/lib/cn'

/**
 * Рукописный знак «AWW».
 *
 * Нарисован обводкой, а не залитой формой: перо одной толщины по всему знаку,
 * и `vector-effect: non-scaling-stroke` держит её одинаковой в любом размере -
 * в шапке в 20px и в подвале в 24px штрих выглядит одним и тем же.
 *
 * Цвет берётся из `currentColor`, поэтому знак сам подхватывает контекст:
 * белый поверх иллюстрации героя, тушь на светлых страницах, светлый в тёмной
 * теме. Отдельной перекраски под тему ему не нужно.
 *
 * Читается знак как «AWW», но подписан полным именем: сокращение - графика,
 * а не название, и скринридеру с поиском нужно второе.
 */
export function Logo({ className }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 300 120"
      fill="none"
      // Пропорция задана здесь, а не в месте вызова: с одной лишь высотой и
      // width:auto внутри контейнера по содержимому ширина зависит от
      // контейнера, а контейнер - от неё, и знак схлопывается в ноль.
      // aspect-ratio разрывает эту петлю - снаружи достаточно задать высоту.
      className={cn('aspect-[5/2] w-auto shrink-0', className)}
      role="img"
      aria-label="awwwdde"
    >
      <g
        stroke="currentColor"
        strokeWidth={7.5}
        strokeLinecap="round"
        strokeLinejoin="round"
        vectorEffect="non-scaling-stroke"
      >
        {/* A: две стойки и перекладина. Перекладина чуть завалена - от руки
            она никогда не ложится строго горизонтально. */}
        <path d="M14 95 C20 70 31 43 43 25" />
        <path d="M43 25 C53 45 63 72 71 95" />
        <path d="M25 77 C37 72 51 71 62 74" />

        {/* W, W: зигзаг с лёгкой кривизной в каждом штрихе. Прямые линии
            выдали бы вектор, а знак должен читаться написанным. */}
        <path d="M90 27 C96 50 105 76 113 94 C119 78 126 58 132 46 C138 62 145 80 152 94 C160 74 167 48 173 27" />
        <path d="M186 27 C192 50 201 76 209 94 C215 78 222 58 228 46 C234 62 241 80 248 94 C256 74 263 48 269 27" />
      </g>
    </svg>
  )
}
