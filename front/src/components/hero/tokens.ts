/** Служебный слой поверх иллюстрации: шапка и подписи героя.
 *
 *  Картинка одинаковая в обеих темах, поэтому цвет здесь не берётся из токенов
 *  темы. Поверх сцены лежит тёмная заливка, а по ней идёт белый текст: одна
 *  плотность на весь кадр вместо приёмов вокруг отдельных букв.
 *  Отдельный модуль нужен ещё и затем, чтобы шапка не тянула в свой бандл всю
 *  сцену со слоями. */
export const HERO_FG = '#ffffff'

/** Плотность заливки. 0.38 - минимум, при котором белый текст держит 4.5:1 над
 *  самым светлым участком газона; ниже контраст уходит под норму. */
export const HERO_VEIL = 0.34
